module uim.exceptions.interfaces.logger;

interface IExceptionLogger {
    // This interface is used to define the contract for exception loggers.
    // It should be implemented by any class that wants to log exceptions in a specific way.
    // The methods defined here should be implemented in the class that implements this interface.
    // The methods should be public and should not throw any exceptions.

    // void logException(string message);
}